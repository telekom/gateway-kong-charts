<!--
SPDX-FileCopyrightText: 2026 Deutsche Telekom AG

SPDX-License-Identifier: CC0-1.0
-->

# AGENTS.md — gateway-kong-charts

## Project

- Helm chart deploying the Kong-based API gateway (Open Telekom Integration Platform).

## Docs generation

- `README.md` is GENERATED by helm-docs from `README.md.gotmpl` + `values.yaml`. Never edit `README.md` directly.
- Regenerate: `helm-docs`
- CI enforces an up-to-date README via `helm-docs`.

## Conventions

- REUSE-compliant: every file needs SPDX headers.
  - License headers: `SPDX-FileCopyrightText: <year> Deutsche Telekom AG` / `SPDX-License-Identifier: CC0-1.0` (docs/config).
- Conventional Commits.
- Git hooks via [`lefthook`](https://lefthook.dev/). Opt-in by running `lefthook install`. The hooks are optional local tooling; CI remains the authoritative gate.
If a hook fails, evaluate the error message critically, report your conclusion and fix or skip it.

## Repo host / tooling

- Remote is GitHub (telekom/gateway-kong-charts) — use `gh`, NOT `glab`, despite global GitLab defaults.
- This checkout is a git submodule/worktree: `.git` is a file; hooks live under the parent repo's `.git/modules/.../hooks/`.
- Validate before done: `helm lint .` and `helm-docs` (no resulting diff).
