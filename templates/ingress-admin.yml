{{/*
SPDX-FileCopyrightText: 2023-2025 Deutsche Telekom AG

SPDX-License-Identifier: Apache-2.0
*/}}

{{- if and .Values.adminApi.enabled .Values.adminApi.ingress.enabled }}
{{/* Route through Kong's proxy listener so the protected /admin-api route handles the request. */}}
{{- $svcPort := include "kong.proxy.portName" . -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-admin
  labels:
    {{- include "app.labels.standard" $ | nindent 4 }}
{{- include "kong.merged.adminApi.annotations" $ | nindent 2 }}
spec:
{{- if or .Values.adminApi.ingress.className .Values.global.ingress.ingressClassName }}
  ingressClassName: {{ .Values.adminApi.ingress.className | default .Values.global.ingress.ingressClassName }}
{{- end }}
  {{- if .Values.adminApi.ingress.tls }}
  tls:
    {{- range .Values.adminApi.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.adminApi.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ $.Release.Name }}-proxy
                port:
                  name: {{ $svcPort }}
          {{- end }}
    {{- end }}
{{- end }}
